/* ==========================================================================
   校园公共信息与数据展示中心 · 交互逻辑
   命名约定：data（数据处理） / render（渲染） / bindEvents（事件）
   第一次提交（骨架阶段）：仅处理页脚年份；筛选与图表在后续提交加入
   ========================================================================== */
(function () {
  'use strict';

  // 页脚年份：纯展示逻辑，不依赖任何第三方库（断网时仍可执行）
  var yearEl = document.getElementById('year');
  if (yearEl) {
    yearEl.textContent = String(new Date().getFullYear());
  }
})();
